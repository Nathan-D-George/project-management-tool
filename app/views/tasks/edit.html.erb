<%= render 'page_topper' %>

<div class='row' style='padding-top:30px'>
  <div class='col-4'></div>
    <div class='col-md-4'>
      <h1>Edit Task</h1>
      <%= render 'form' %>
    </div>
  </div>
  <%= link_to "Delete Task", destroy_task_path(id: @task.id), data:{turbo_method: :delete, turbo_confirm: "Delete #{@task.name}"},class:'btn', style:'color:white; background-color: red'  %>
</div>

 